import React from 'react';
import DriverMessaging from '@/components/driver-messaging';

// Componente refatorado que usa a estrutura modular
const DashboardDriverMessaging: React.FC = () => {
  return <DriverMessaging />;
};

export default DashboardDriverMessaging;